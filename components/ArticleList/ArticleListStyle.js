import styled from 'styled-components'
import Grid from '@mui/material/Grid';
import Paper from '@mui/material/Paper';

export const ArticleCard = styled(Paper)`
    backgroundColor: '#1A2027'
    padding: theme.spacing(1);
    textAlign: 'center';
`;
